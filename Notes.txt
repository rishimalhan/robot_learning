### Conda use ###
catkin config \
  --install \
  --install-space $CONDA_PREFIX/install \
  --devel-space $CONDA_PREFIX/devel \
  --build-space $CONDA_PREFIX/build \
  --log-space $CONDA_PREFIX/logs \
  --cmake-args -DCMAKE_INSTALL_PREFIX=$CONDA_PREFIX/install

### catkin commands ###
wstool init /robot_learning/src/deps/ /robot_learning/.rosinstall
catkin config --jobs 14